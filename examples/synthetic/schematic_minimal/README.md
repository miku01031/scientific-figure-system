# Minimal synthetic schematic

Run `python examples/synthetic/schematic_minimal/run_example.py` from the
repository root. It always writes a native editable `output/diagram.drawio`.
draw.io Desktop is optional; if discovered, application export is an additional
capability. The diagram is generic demonstration content, not a research method.
