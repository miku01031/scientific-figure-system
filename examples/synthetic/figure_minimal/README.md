# Minimal synthetic figure

Run from the repository root with `python examples/synthetic/figure_minimal/run_example.py`.
The launcher resolves the relative `data.csv` path and computes its SHA-256
provenance. It writes an SVG, plus PDF/PNG only when native Cairo is available.
This is demonstration data, not scientific evidence.
