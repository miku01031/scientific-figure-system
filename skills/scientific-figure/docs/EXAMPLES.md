# Examples

The runnable stranger-first example is in
`examples/synthetic/figure_minimal/`. It is deterministic, uses a relative
source path, and is not scientific evidence. For real data, create a separate
mapping ledger and SHA-256 provenance record; never copy a research script into
the examples or tests.
