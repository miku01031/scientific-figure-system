# Design language

The public candidate uses conservative publication-oriented defaults for the
three supported renderers. Layout is deterministic but not universal; legends,
markers and whitespace are measured for the supplied semantic spec. Scientific
meaning and frozen arrays remain authoritative. This document describes the
current product, not private experiment history.
