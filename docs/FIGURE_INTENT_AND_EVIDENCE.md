# Lightweight planning contracts

`schemas/FIGURE_INTENT_CONTRACT.schema.json` is optional planning metadata. The minimum useful fields are `scientific_question`, `figure_job`, and `primary_message`; it is not a bureaucratic production gate. `schemas/MULTIPANEL_EVIDENCE_CONTRACT.schema.json` records each panel role without implementing an automatic multi-panel designer.
